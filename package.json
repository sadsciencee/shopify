{
  "name": "@sadsciencee/shopify-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test"
  },
  "workspaces": [
    "packages/*"
  ]
}
